/**
 * @interface IGameService
 *
 * startGame(config: { players: Player[] }): void
 * rollDice(): void
 * makeMove({ row: number, col: number }): void
 * resetGame(): void
 *
 * state: reactive snapshot of the current game state
 */
